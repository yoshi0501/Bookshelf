<div class="mb-8 flex justify-between items-center">
  <div>
    <h1 class="text-2xl font-bold text-gray-900">発注承認依頼: <%= @order.order_no %></h1>
    <p class="mt-1 text-sm text-gray-500">依頼日時: <%= @order_approval_request.created_at.strftime("%Y年%m月%d日 %H:%M") %></p>
  </div>
  <div class="flex space-x-3">
    <%= link_to "発注一覧に戻る", order_approval_requests_path, class: "text-sm font-medium text-indigo-600 hover:text-indigo-500" %>
  </div>
</div>

<% if @order_approval_request.status_pending? %>
  <div class="bg-yellow-50 border-l-4 border-yellow-400 p-4 mb-6">
    <div class="flex">
      <div class="flex-shrink-0">
        <svg class="h-5 w-5 text-yellow-400" viewBox="0 0 20 20" fill="currentColor">
          <path fill-rule="evenodd" d="M8.257 3.099c.765-1.36 2.722-1.36 3.486 0l5.58 9.92c.75 1.334-.213 2.98-1.742 2.98H4.42c-1.53 0-2.493-1.646-1.743-2.98l5.58-9.92zM11 13a1 1 0 11-2 0 1 1 0 012 0zm-1-8a1 1 0 00-1 1v3a1 1 0 002 0V6a1 1 0 00-1-1z" clip-rule="evenodd" />
        </svg>
      </div>
      <div class="ml-3">
        <p class="text-sm text-yellow-700">
          この発注は承認待ちです。内容を確認して承認または却下してください。
        </p>
      </div>
    </div>
  </div>
<% end %>

<div class="grid grid-cols-1 gap-6 lg:grid-cols-2 mb-6">
  <div class="bg-white shadow rounded-lg overflow-hidden">
    <div class="px-4 py-5 sm:px-6">
      <h3 class="text-lg font-medium leading-6 text-gray-900">発注情報</h3>
    </div>
    <div class="border-t border-gray-200">
      <dl>
        <div class="bg-gray-50 px-4 py-3 sm:grid sm:grid-cols-3 sm:gap-4 sm:px-6">
          <dt class="text-sm font-medium text-gray-500">発注番号</dt>
          <dd class="mt-1 text-sm text-gray-900 sm:col-span-2 sm:mt-0"><%= @order.order_no %></dd>
        </div>
        <div class="bg-white px-4 py-3 sm:grid sm:grid-cols-3 sm:gap-4 sm:px-6">
          <dt class="text-sm font-medium text-gray-500">発注日</dt>
          <dd class="mt-1 text-sm text-gray-900 sm:col-span-2 sm:mt-0"><%= @order.order_date %></dd>
        </div>
        <div class="bg-gray-50 px-4 py-3 sm:grid sm:grid-cols-3 sm:gap-4 sm:px-6">
          <dt class="text-sm font-medium text-gray-500">発注者</dt>
          <dd class="mt-1 text-sm text-gray-900 sm:col-span-2 sm:mt-0"><%= @order.ordered_by_user.name || @order.ordered_by_user.email %></dd>
        </div>
        <div class="bg-white px-4 py-3 sm:grid sm:grid-cols-3 sm:gap-4 sm:px-6">
          <dt class="text-sm font-medium text-gray-500">合計金額</dt>
          <dd class="mt-1 text-sm text-gray-900 sm:col-span-2 sm:mt-0 font-semibold">
            ¥<%= number_to_currency(@order.total_amount, unit: "", precision: 0) %>
          </dd>
        </div>
        <div class="bg-gray-50 px-4 py-3 sm:grid sm:grid-cols-3 sm:gap-4 sm:px-6">
          <dt class="text-sm font-medium text-gray-500">CO2合計</dt>
          <dd class="mt-1 text-sm text-gray-900 sm:col-span-2 sm:mt-0"><%= @order.co2_total %> kg</dd>
        </div>
      </dl>
    </div>
  </div>

  <div class="bg-white shadow rounded-lg overflow-hidden">
    <div class="px-4 py-5 sm:px-6">
      <h3 class="text-lg font-medium leading-6 text-gray-900">配送先情報</h3>
    </div>
    <div class="border-t border-gray-200">
      <dl>
        <div class="bg-gray-50 px-4 py-3 sm:grid sm:grid-cols-3 sm:gap-4 sm:px-6">
          <dt class="text-sm font-medium text-gray-500">顧客</dt>
          <dd class="mt-1 text-sm text-gray-900 sm:col-span-2 sm:mt-0"><%= @order.customer.display_name %></dd>
        </div>
        <div class="bg-white px-4 py-3 sm:grid sm:grid-cols-3 sm:gap-4 sm:px-6">
          <dt class="text-sm font-medium text-gray-500">配送先</dt>
          <dd class="mt-1 text-sm text-gray-900 sm:col-span-2 sm:mt-0"><%= @order.full_shipping_address %></dd>
        </div>
      </dl>
    </div>
  </div>
</div>

<div class="bg-white shadow rounded-lg overflow-hidden mb-6">
  <div class="px-4 py-5 sm:px-6">
    <h3 class="text-lg font-medium leading-6 text-gray-900">発注明細</h3>
  </div>
  <div class="border-t border-gray-200">
    <table class="min-w-full divide-y divide-gray-200">
      <thead class="bg-gray-50">
        <tr>
          <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">商品コード</th>
          <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">商品名</th>
          <th class="px-6 py-3 text-right text-xs font-medium text-gray-500 uppercase tracking-wider">数量</th>
          <th class="px-6 py-3 text-right text-xs font-medium text-gray-500 uppercase tracking-wider">単価</th>
          <th class="px-6 py-3 text-right text-xs font-medium text-gray-500 uppercase tracking-wider">金額</th>
        </tr>
      </thead>
      <tbody class="bg-white divide-y divide-gray-200">
        <% @order.order_lines.each do |line| %>
          <tr>
            <td class="px-6 py-4 whitespace-nowrap text-sm font-medium text-gray-900"><%= line.item_code %></td>
            <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-900"><%= line.item_name %></td>
            <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-900 text-right"><%= line.quantity %></td>
            <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-500 text-right">
              ¥<%= number_to_currency(line.unit_price_snapshot, unit: "", precision: 0) %>
            </td>
            <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-900 text-right font-semibold">
              ¥<%= number_to_currency(line.amount, unit: "", precision: 0) %>
            </td>
          </tr>
        <% end %>
      </tbody>
      <tfoot class="bg-gray-50">
        <tr>
          <td colspan="4" class="px-6 py-4 text-sm font-medium text-gray-900 text-right">合計:</td>
          <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-900 text-right font-bold">
            ¥<%= number_to_currency(@order.total_amount, unit: "", precision: 0) %>
          </td>
        </tr>
      </tfoot>
    </table>
  </div>
</div>

<% if @order_approval_request.status_pending? %>
  <div class="bg-white shadow rounded-lg p-6">
    <h3 class="text-lg font-medium leading-6 text-gray-900 mb-4">承認アクション</h3>
    <div class="flex flex-wrap gap-3">
      <%= form_with url: approve_order_approval_request_path(@order_approval_request), method: :post, class: "inline" do %>
        <%= button_tag "承認する", type: "submit", class: "rounded-md bg-green-600 px-4 py-2 text-sm font-semibold text-white shadow-sm hover:bg-green-500", data: { confirm: "この発注を承認しますか？" } %>
      <% end %>
      
      <%= form_with url: reject_order_approval_request_path(@order_approval_request), method: :post, class: "inline" do |f| %>
        <div class="flex items-center space-x-2">
          <%= f.text_field :comment, placeholder: "却下理由（任意）", class: "rounded-md border-gray-300 shadow-sm focus:border-red-500 focus:ring-red-500 sm:text-sm" %>
          <%= button_tag "却下する", type: "submit", class: "rounded-md bg-red-600 px-4 py-2 text-sm font-semibold text-white shadow-sm hover:bg-red-500", data: { confirm: "この発注を却下しますか？" } %>
        </div>
      <% end %>
    </div>
  </div>
<% elsif @order_approval_request.status_approved? %>
  <div class="bg-green-50 border-l-4 border-green-400 p-4 mb-6">
    <div class="flex">
      <div class="flex-shrink-0">
        <svg class="h-5 w-5 text-green-400" viewBox="0 0 20 20" fill="currentColor">
          <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd" />
        </svg>
      </div>
      <div class="ml-3">
        <p class="text-sm text-green-700">
          この発注は承認済みです。
          <% if @order_approval_request.reviewed_by %>
            承認者: <%= @order_approval_request.reviewed_by.name || @order_approval_request.reviewed_by.email %>
            (<%= @order_approval_request.reviewed_at.strftime("%Y年%m月%d日 %H:%M") %>)
          <% end %>
        </p>
      </div>
    </div>
  </div>
<% elsif @order_approval_request.status_rejected? %>
  <div class="bg-red-50 border-l-4 border-red-400 p-4 mb-6">
    <div class="flex">
      <div class="flex-shrink-0">
        <svg class="h-5 w-5 text-red-400" viewBox="0 0 20 20" fill="currentColor">
          <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zM8.707 7.293a1 1 0 00-1.414 1.414L8.586 10l-1.293 1.293a1 1 0 101.414 1.414L10 11.414l1.293 1.293a1 1 0 001.414-1.414L11.414 10l1.293-1.293a1 1 0 00-1.414-1.414L10 8.586 8.707 7.293z" clip-rule="evenodd" />
        </svg>
      </div>
      <div class="ml-3">
        <p class="text-sm text-red-700">
          この発注は却下されました。
          <% if @order_approval_request.reviewed_by %>
            却下者: <%= @order_approval_request.reviewed_by.name || @order_approval_request.reviewed_by.email %>
            (<%= @order_approval_request.reviewed_at.strftime("%Y年%m月%d日 %H:%M") %>)
          <% end %>
          <% if @order_approval_request.review_comment.present? %>
            <br>理由: <%= @order_approval_request.review_comment %>
          <% end %>
        </p>
      </div>
    </div>
  </div>
<% end %>
