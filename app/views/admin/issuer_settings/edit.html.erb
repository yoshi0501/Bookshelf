<div class="d-flex justify-content-between align-items-center mb-4">
  <div>
    <h1 class="h2 fw-bold mb-2"><%= t('issuer_settings.edit.title') %></h1>
    <p class="text-muted mb-0"><%= t('issuer_settings.edit.description') %></p>
  </div>
  <%= link_to admin_issuer_setting_path, class: "btn btn-outline-secondary btn-sm" do %>
    <i class="bi bi-arrow-left me-1"></i><%= t('shared.actions.back') %>
  <% end %>
</div>

<%= render "form", issuer_setting: @issuer_setting %>
