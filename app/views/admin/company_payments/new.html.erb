<div class="d-flex justify-content-between align-items-center mb-4">
  <div>
    <h1 class="h2 fw-bold mb-2"><%= t('company_payments.new.title') %></h1>
    <p class="text-muted mb-0"><%= t('company_payments.new.description') %></p>
  </div>
  <%= link_to admin_company_payments_path, class: "btn btn-outline-secondary btn-sm" do %>
    <i class="bi bi-arrow-left me-1"></i><%= t('shared.actions.back') %>
  <% end %>
</div>

<div class="card shadow-sm border-0">
  <div class="card-body">
    <%= render "form", company_payment: @company_payment %>
  </div>
</div>
