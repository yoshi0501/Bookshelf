<div class="d-flex justify-content-between align-items-center mb-4">
  <div>
    <h1 class="h2 fw-bold mb-2"><%= t('manufacturers.edit.title') %></h1>
    <p class="text-muted mb-0"><%= t('manufacturers.edit.description') %></p>
  </div>
  <%= link_to manufacturer_path(@manufacturer), class: "btn btn-outline-primary btn-sm" do %>
    <i class="bi bi-arrow-left me-1"></i><%= t('shared.actions.back') %>
  <% end %>
</div>
<%= render "form", manufacturer: @manufacturer %>
